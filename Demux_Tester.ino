#define ADDRESS1 22
#define ADDRESS2 24
#define ADDRESS3 26

int state = 0;

void setup() {
    pinMode(ADDRESS1, OUTPUT);
    pinMode(ADDRESS2, OUTPUT);
    pinMode(ADDRESS3, OUTPUT);
}

void loop {

}